api:
  dashboard: true
  insecure: true

log:
  level: INFO

accessLog:
  format: json

entryPoints:
  web:
    address: ":80"
  metrics:
    address: ":8082"

metrics:
  prometheus:
    entryPoint: metrics
    addEntryPointsLabels: true
    addRoutersLabels: true
    addServicesLabels: true

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: nutriplanner
  file:
    directory: /etc/traefik/dynamic
    watch: true