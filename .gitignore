#  Environnements virtuels
.venv/
venv/
# venvs nommés env/ : pattern scopé, un `env/` nu avalerait infra/env/ (conf committée)
/env/
service-*/env/

#  Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

#  Packaging
build/
dist/
*.egg-info/

#  Tests / coverage
.coverage
htmlcov/
.pytest_cache/
coverage.xml

#  IDE
.idea/
.vscode/
*.code-workspace
#  Fichiers d'environnement (secrets)
.env
.env.*
!.env.example
!.env.*.example
.secrets
# Secrets stratifiés (phase 2 infra) — seul le README est committé
infra/env/secrets/*
!infra/env/secrets/README.md
#  Dumps Postgres (données + hash de mots de passe) — jamais versionnés
infra/postgres/dump/
#  Docker (optionnel si tu en as)
*.log
docker-compose.override.yml

#tests ci
bin/

#  OS
.DS_Store
Thumbs.db

# Celery Beat
celerybeat-schedule
celerybeat.pid

#local ci
ci-local*
bandit*

#front
frontend/

SECURITY*

# HTTPS dev — certificats mkcert (ne jamais commiter la clé privée)
infra/traefik/certs/

#  Docs / études internes (non versionnées)
docs/
