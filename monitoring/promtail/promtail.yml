server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: nutriplanner-docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      - source_labels: [__meta_docker_container_name]
        regex: '/nutriplanner-.*'
        action: keep
      - source_labels: [__meta_docker_container_name]
        regex: '/?(.+)'
        target_label: container
      - source_labels: [__meta_docker_container_label_com_docker_compose_service]
        target_label: service
      - source_labels: [service]
        regex: '^$'
        target_label: service
        replacement: 'unknown'
    pipeline_stages:
      - json:
          expressions:
            level: level
            event: event
            trace_id: trace_id
            traefik_status: DownstreamStatus
            traefik_method: RequestMethod
            traefik_path: RequestPath
      - labels:
          level:
          trace_id:
      - output:
          source: event
